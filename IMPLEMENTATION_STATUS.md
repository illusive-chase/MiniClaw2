# 实现状态

## 工作区通知边界

工作区通知流会实时报告当前后端进程引起的节点状态变化。经 Git sync 从其他
设备合入的节点变化，以及共享同一 store 的其他进程写入的变化，不会产生通知
事件；它们会在下一次显式读取时可见，例如打开项目、刷新 landing 页或请求
`/active-nodes`。

这与 peer state 模型一致：同步得到的远端状态是采集时刻的快照，不是实时事件源。

同一后端的多个客户端（多个浏览器标签页、外部 API 调用方）并发产生的项目级变更
——创建、改名、删除、绑定、打标签——同样不进通知流：事件词汇表只有节点级的
`workspace_node_updated` 与 `workspace_node_removed`，没有项目级事件。单个标签页
内自己发起的改动会本地立即生效，其他客户端的改动则在失焦/回焦或下一次显式读取时
对账。这是有意的范围边界，不是缺陷——多客户端并发不在当前支持范围内。
