Synthesize the human reviewer's prose with the calculator review brief.

If the human finds the result acceptable, write your own review
`preview.json` and do not propose any virtual follow-up nodes. If they
ask for fixes, write your own review `preview.json` and create one
regular virtual preview that implements the requested fixes, depending
on the build node.
