Read the human reviewer's prose and the build node's preview,
transcript, and artifacts.

Write your own review `preview.json`. If the human asks for
`subtract(a, b)`, create one regular virtual preview for that fix with
the build node as its dependency. This template also declares a concrete
resume-based `fix` node, so keep the virtual brief and focused rather
than expanding the plan.
