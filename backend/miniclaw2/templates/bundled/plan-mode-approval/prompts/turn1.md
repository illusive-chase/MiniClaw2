Create a file at the project root named `PLAN_OK.txt` containing
exactly the single line `plan-approved` followed by a newline. Do not
create any other files.

You are in plan mode — first propose a brief plan for this single
write, then wait for me to approve it. Once I approve, perform the
write. After the file exists, confirm in one short sentence that you
wrote it.
