Simplest end-to-end run: agent produces a coherent one-sentence reply
ending with the literal token `[OK]`. No tool use, no files written.
Validates the WS protocol, provider adapter loop, and assistant text
rendering across both providers.
