Create a file at `README.md` in the project root containing exactly the
single line `# scratch` followed by a newline. Do not create any other
files. Do not write any other content into `README.md`. After writing
the file, confirm in one short sentence that you wrote it.
